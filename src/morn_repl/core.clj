(ns morn-repl.core
  (:gen-class)
  (:require [clojure.string :as string]
            [morn-repl.term-parser :as term-parser]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println args))